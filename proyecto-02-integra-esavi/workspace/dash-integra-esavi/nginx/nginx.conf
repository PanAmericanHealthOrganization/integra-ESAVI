# ── Redirigir todo HTTP → HTTPS ───────────────────────────────────────────────
server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}

# ── HTTPS principal ───────────────────────────────────────────────────────────
server {
    listen 443 ssl;
    server_name _;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # Shiny necesita WebSockets con timeouts largos
    proxy_read_timeout  3600s;
    proxy_send_timeout  3600s;

    location / {
        # Hacia oauth2-proxy, no directo al dashboard: es quien aplica la
        # autenticación Keycloak y los --allowed-role. Además es el único
        # alcanzable desde aquí (nginx solo está en la red esavi-public,
        # dashboard vive en default + esavi-internal).
        proxy_pass         http://oauth2-proxy:4180;
        proxy_http_version 1.1;
        proxy_set_header   Upgrade    $http_upgrade;
        proxy_set_header   Connection "upgrade";
        proxy_set_header   Host       $host;
        proxy_set_header   X-Real-IP  $remote_addr;
        proxy_set_header   X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }
}
