# Sobrescribe docker-compose.yml para desarrollo local sin autenticación Keycloak.
# Uso: docker compose -f docker-compose.yml -f docker-compose.local.yml up -d --build
services:
  oauth2-proxy:
    profiles: ["auth"]   # desactiva oauth2-proxy en local
